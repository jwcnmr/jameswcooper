#pragma once
#include <sqlite3.h>
#include <string>
#include <vector>
#include <map>

using std::string;
using std::vector;
using std::tuple;
using std::map;

//convenient abbreviation for the map
typedef  map<string, string> dbMap;
